import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    width: "14.5em",
    borderRadius: "1.5em",
    overflow: "unset",
  },
  media: {
    height: "27.06em",
    position: "relative",
  },
});
